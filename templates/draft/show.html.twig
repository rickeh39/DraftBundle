{% extends 'base.html.twig' %}

{% block title %}Hello Draft!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ draft.title }} ✅</h1>


        Dit zijn al je gegevens
        <ul>

            <li>{{ draft.description }}</li>
            <li>{{ draft.content }}</li>
            <li>{{ draft.user }}</li>
        </ul>

        <a href="{{ path('draft_edit',{'id': draft.id}) }}">{{ draft.title }} bewerken</a>
        <a href="{{ path('draft_publish',{'id': draft.id}) }}">Publiseren</a>
    </div>
{% endblock %}
